{
  "name": "jquery.keep-ratio",
  "description": "A jQuery plugin for keeping the aspect ratio",
  "version": "0.1.1",
  "repository": {
    "type": "git",
    "url": "git@github.com:loonkwil/jquery.keep-ratio.git"
  },
  "engines": {
    "node": ">= 0.10.0"
  },
  "scripts": {
    "test": "gulp test"
  },
  "devDependencies": {
    "gulp": "^3.8.0",
    "gulp-bump": "^0.1.0",
    "gulp-git": "^0.5.0",
    "gulp-jshint": "^1.8.0",
    "gulp-load-plugins": "^0.6.0",
    "gulp-qunit": "^0.3.0",
    "nopt": "^3.0.0",
    "gulp-jscs": "^1.1.0",
    "gulp-jsonlint": "^0.1.0",
    "gulp-concat": "^2.4.0",
    "gulp-uglify": "^1.0.0"
  }
}
